name: senlab

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - senlab-net
    ports:
      - "5432:5432"

  traefik:
    image: traefik:v3.1
    restart: unless-stopped
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --api.dashboard=true
      - --api.insecure=true
      - --log.level=INFO
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - senlab-net

  # Placeholders activés au fur et à mesure de Phase 0.
  # Décommenter après build des images locales.
  #
  # auth-service:
  #   build: ../../sen-lab-auth-service
  #   env_file: ../.env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks: [senlab-net]
  #   labels:
  #     - traefik.enable=true
  #     - traefik.http.routers.auth.rule=Host(`auth.${TRAEFIK_DOMAIN}`)
  #
  # users-service:
  #   build: ../../sen-lab-users-service
  #   env_file: ../.env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks: [senlab-net]
  #
  # gateway:
  #   build: ../../sen-lab-gateway
  #   env_file: ../.env
  #   depends_on: [auth-service, users-service]
  #   networks: [senlab-net]
  #   labels:
  #     - traefik.enable=true
  #     - traefik.http.routers.api.rule=Host(`api.${TRAEFIK_DOMAIN}`)
  #     - traefik.http.services.api.loadbalancer.server.port=3000
  #
  # web:
  #   build: ../../sen-lab-web
  #   env_file: ../.env
  #   depends_on: [gateway]
  #   networks: [senlab-net]
  #   labels:
  #     - traefik.enable=true
  #     - traefik.http.routers.web.rule=Host(`${TRAEFIK_DOMAIN}`)
  #     - traefik.http.services.web.loadbalancer.server.port=3000

volumes:
  pgdata:

networks:
  senlab-net:
    driver: bridge
